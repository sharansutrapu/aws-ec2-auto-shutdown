# ec2-auto-shutdown
AWS Lambda function that automatically stops EC2 instances on a daily or weekly schedule using instance tags. Sends Slack notifications and publishes CloudWatch metrics on each run.
